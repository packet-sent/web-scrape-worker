{
  "private": true,
  "name": "web-scrape-worker",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "speedproxies <support@speedproxies.net>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
